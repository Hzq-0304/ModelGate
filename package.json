{
  "name": "modelgate",
  "version": "0.1.0",
  "description": "Local OpenAI-compatible model request router and provider hot-switch gateway.",
  "type": "module",
  "private": false,
  "bin": {
    "modelgate": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "cli": "tsx src/cli.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "yaml": "^2.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
