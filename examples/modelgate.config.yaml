server:
  host: 127.0.0.1
  port: 11435

active: mock-main

entrypoints:
  codex-main:
    use: active

aliases:
  mock-main:
    provider: mock
    model: mock-codex-model

  # deepseek-main:
  #   provider: deepseek
  #   model: deepseek-chat

  # qwen-main:
  #   provider: qwen
  #   model: qwen-plus

providers:
  mock:
    type: mock

  # deepseek:
  #   type: openai-compatible
  #   base_url: https://api.deepseek.com/v1
  #   api_key: ${DEEPSEEK_API_KEY}
  #   responses_api: false

  # qwen:
  #   type: openai-compatible
  #   base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  #   api_key: ${QWEN_API_KEY}
  #   responses_api: false

  # openai:
  #   type: openai-compatible
  #   base_url: https://api.openai.com/v1
  #   api_key: ${OPENAI_API_KEY}
  #   responses_api: true

# Optional estimated-cost pricing. Verify provider pricing before relying on these examples.
# pricing:
#   deepseek/deepseek-chat:
#     input_per_million: 0.27
#     output_per_million: 1.10
#     cached_input_per_million: 0.07
#
#   qwen/qwen-plus:
#     input_per_million: 0
#     output_per_million: 0
